apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: matmul-api
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: matmul-api
            port: { number: 8080 }
